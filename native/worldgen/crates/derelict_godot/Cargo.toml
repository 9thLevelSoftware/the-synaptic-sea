[package]
name = "derelict_godot"
version.workspace = true
edition.workspace = true
build = "build.rs"

[lib]
crate-type = ["cdylib"]

[dependencies]
derelict_core = { path = "../derelict_core" }
godot = "0.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
derelict_core = { path = "../derelict_core" }
