.F..FFFFFFFFFFFFFFFFFF.....F.......FF..........EEEEEEE......FFFFFFFFFFFF [ 57%]
FFFF....FF.....................FFFFF..FFFFFFFF..FFF..                    [100%]
==================================== ERRORS ====================================
_ ERROR at setup of test_missing_contract_socket_is_reported_deterministically _

    @pytest.fixture
    def BASE_REPORT() -> dict:
>       spec = load_source_spec(PROJECT_ROOT, "floor_1x1")

tests/test_validate_structural_sources.py:26: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_________ ERROR at setup of test_non_identity_source_root_is_rejected __________

    @pytest.fixture
    def BASE_REPORT() -> dict:
>       spec = load_source_spec(PROJECT_ROOT, "floor_1x1")

tests/test_validate_structural_sources.py:26: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
____________ ERROR at setup of test_valid_report_passes_validation _____________

    @pytest.fixture
    def BASE_REPORT() -> dict:
>       spec = load_source_spec(PROJECT_ROOT, "floor_1x1")

tests/test_validate_structural_sources.py:26: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_______ ERROR at setup of test_source_record_with_wrong_hash_is_rejected _______

    @pytest.fixture
    def BASE_REPORT() -> dict:
>       spec = load_source_spec(PROJECT_ROOT, "floor_1x1")

tests/test_validate_structural_sources.py:26: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_______________ ERROR at setup of test_extra_helper_is_reported ________________

    @pytest.fixture
    def BASE_REPORT() -> dict:
>       spec = load_source_spec(PROJECT_ROOT, "floor_1x1")

tests/test_validate_structural_sources.py:26: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
___________ ERROR at setup of test_wrong_collision_shape_is_reported ___________

    @pytest.fixture
    def BASE_REPORT() -> dict:
>       spec = load_source_spec(PROJECT_ROOT, "floor_1x1")

tests/test_validate_structural_sources.py:26: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
______ ERROR at setup of test_report_errors_are_lexicographically_sorted _______

    @pytest.fixture
    def BASE_REPORT() -> dict:
>       spec = load_source_spec(PROJECT_ROOT, "floor_1x1")

tests/test_validate_structural_sources.py:26: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
=================================== FAILURES ===================================
____________ test_floor_contract_exposes_exact_socket_anchor_names _____________

    def test_floor_contract_exposes_exact_socket_anchor_names() -> None:
>       spec = load_source_spec(PROJECT_ROOT, "floor_1x1")

tests/test_structural_source_contract.py:46: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_________ test_all_fifteen_modules_load_from_live_contracts[floor_1x1] _________

module_id = 'floor_1x1'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_________ test_all_fifteen_modules_load_from_live_contracts[floor_2x1] _________

module_id = 'floor_2x1'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_2x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_2x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_2x1/floor_2x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
____ test_all_fifteen_modules_load_from_live_contracts[corridor_floor_1x1] _____

module_id = 'corridor_floor_1x1'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'corridor_floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/corridor_floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/corridor_floor_1x1/corridor_floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
____ test_all_fifteen_modules_load_from_live_contracts[corridor_floor_1x2] _____

module_id = 'corridor_floor_1x2'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'corridor_floor_1x2'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/corridor_floor_1x2_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/corridor_floor_1x2/corridor_floor_1x2.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_____ test_all_fifteen_modules_load_from_live_contracts[wall_straight_1x1] _____

module_id = 'wall_straight_1x1'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'wall_straight_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/wall_straight_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/wall_straight_1x1/wall_straight_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
__ test_all_fifteen_modules_load_from_live_contracts[doorway_frame_open_1x1] ___

module_id = 'doorway_frame_open_1x1'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'doorway_frame_open_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/doorway_frame_open_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/doorway_frame_open_1x1/doorway_frame_open_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
____ test_all_fifteen_modules_load_from_live_contracts[pillar_support_1x1] _____

module_id = 'pillar_support_1x1'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'pillar_support_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/pillar_support_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/pillar_support_1x1/pillar_support_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
________ test_all_fifteen_modules_load_from_live_contracts[ramp_up_1x2] ________

module_id = 'ramp_up_1x2'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'ramp_up_1x2'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/ramp_up_1x2_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/ramp_up_1x2/ramp_up_1x2.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
____ test_all_fifteen_modules_load_from_live_contracts[bulkhead_portal_2x1] ____

module_id = 'bulkhead_portal_2x1'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'bulkhead_portal_2x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/bulkhead_portal_2x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/bulkhead_portal_2x1/bulkhead_portal_2x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
______ test_all_fifteen_modules_load_from_live_contracts[ceiling_cap_1x1] ______

module_id = 'ceiling_cap_1x1'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'ceiling_cap_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/ceiling_cap_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/ceiling_cap_1x1/ceiling_cap_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_ test_all_fifteen_modules_load_from_live_contracts[doorway_frame_blocked_1x1] _

module_id = 'doorway_frame_blocked_1x1'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'doorway_frame_blocked_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/doorway_frame_blocked_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/doorway_frame_blocked_1x1/doorway_frame_blocked_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_______ test_all_fifteen_modules_load_from_live_contracts[wall_end_cap] ________

module_id = 'wall_end_cap'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'wall_end_cap'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/wall_end_cap_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/wall_end_cap/wall_end_cap.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_____ test_all_fifteen_modules_load_from_live_contracts[wall_inner_corner] _____

module_id = 'wall_inner_corner'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'wall_inner_corner'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/wall_inner_corner_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/wall_inner_corner/wall_inner_corner.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_____ test_all_fifteen_modules_load_from_live_contracts[wall_outer_corner] _____

module_id = 'wall_outer_corner'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'wall_outer_corner'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/wall_outer_corner_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/wall_outer_corner/wall_outer_corner.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
______ test_all_fifteen_modules_load_from_live_contracts[wall_t_junction] ______

module_id = 'wall_t_junction'

    @pytest.mark.parametrize("module_id", [
        "floor_1x1",
        "floor_2x1",
        "corridor_floor_1x1",
        "corridor_floor_1x2",
        "wall_straight_1x1",
        "doorway_frame_open_1x1",
        "pillar_support_1x1",
        "ramp_up_1x2",
        "bulkhead_portal_2x1",
        "ceiling_cap_1x1",
        "doorway_frame_blocked_1x1",
        "wall_end_cap",
        "wall_inner_corner",
        "wall_outer_corner",
        "wall_t_junction",
    ])
    def test_all_fifteen_modules_load_from_live_contracts(module_id: str) -> None:
>       spec = load_source_spec(PROJECT_ROOT, module_id)

tests/test_structural_source_contract.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'wall_t_junction'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/wall_t_junction_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/wall_t_junction/wall_t_junction.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
____________ test_source_record_is_canonical_and_has_no_clock_field ____________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_source_record_is_canonica0')

    def test_source_record_is_canonical_and_has_no_clock_field(tmp_path: Path) -> None:
>       spec = load_source_spec(PROJECT_ROOT, "floor_1x1")

tests/test_structural_source_contract.py:109: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
____________________ test_contract_sha256_is_raw_file_hash _____________________

    def test_contract_sha256_is_raw_file_hash() -> None:
>       spec = load_source_spec(PROJECT_ROOT, "floor_1x1")

tests/test_structural_source_contract.py:126: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
__________ test_socket_z_up_positions_match_contract_after_conversion __________

    def test_socket_z_up_positions_match_contract_after_conversion() -> None:
>       spec = load_source_spec(PROJECT_ROOT, "floor_1x1")

tests/test_structural_source_contract.py:133: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
______________________ test_inverted_bounds_are_rejected _______________________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_inverted_bounds_are_rejec0')

    def test_inverted_bounds_are_rejected(tmp_path: Path) -> None:
        document = json.loads((FIXTURE_ROOT / "floor_1x1_contract.json").read_text())
        document["bounds"]["local_min_m"][0] = 3.0
        _write_floor_fixture(tmp_path, document)
    
        with pytest.raises(ValueError, match="bounds"):
>           load_source_spec(tmp_path, "floor_1x1")

tests/test_structural_source_contract.py:186: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:302: in load_source_spec
    return _load_spec_from_paths(root, module_id, contract_path, source_glb_path)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_inverted_bounds_are_rejec0')
module_id = 'floor_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est-27/test_inverted_bounds_are_rejec0/data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...ughby/pytest-27/test_inverted_bounds_are_rejec0/assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
__________ test_pressure_door_candidate_reuses_doorway_contract_shape __________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_pressure_door_candidate_r0')

    def test_pressure_door_candidate_reuses_doorway_contract_shape(tmp_path: Path) -> None:
        staged_path = _write_pressure_door_fixture(tmp_path)
    
>       spec = load_candidate_source_spec(
            tmp_path, "pressure_door_1x1", CANDIDATE_GLB_RELATIVE
        )

tests/test_structural_source_contract.py:284: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_pressure_door_candidate_r0')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_pressure_door_candidate_r0/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_pressure_door_candidate_r0/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
___________ test_pressure_door_candidate_rejects_missing_staged_glb ____________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_pressure_door_candidate_r1')

    def test_pressure_door_candidate_rejects_missing_staged_glb(tmp_path: Path) -> None:
        _write_pressure_door_fixture(tmp_path, with_glb=False)
    
        with pytest.raises(ValueError, match="missing candidate source GLB"):
>           load_candidate_source_spec(tmp_path, "pressure_door_1x1", CANDIDATE_GLB_RELATIVE)

tests/test_structural_source_contract.py:308: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_pressure_door_candidate_r1')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_pressure_door_candidate_r1/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_pressure_door_candidate_r1/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
___ test_candidate_source_record_and_report_validate_without_live_selection ____

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_source_record_a0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10a852520>

    def test_candidate_source_record_and_report_validate_without_live_selection(
        tmp_path: Path, monkeypatch: pytest.MonkeyPatch
    ) -> None:
        _write_candidate_project(tmp_path)
        source_root = tmp_path / "recovered"
        source_dir = source_root / "pressure_door_1x1"
        source_dir.mkdir(parents=True)
        blend_path = source_dir / "pressure_door_1x1.blend"
        blend_path.write_bytes(b"blend fixture")
>       spec = validator.load_candidate_source_spec(
            tmp_path, "pressure_door_1x1", CANDIDATE_GLB_RELATIVE
        )

tests/test_validate_structural_sources.py:327: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_source_record_a0')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_source_record_a0/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_source_record_a0/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
____________ test_candidate_source_record_hash_mismatch_is_rejected ____________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_source_record_h0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10a7f47f0>

    def test_candidate_source_record_hash_mismatch_is_rejected(
        tmp_path: Path, monkeypatch: pytest.MonkeyPatch
    ) -> None:
        _write_candidate_project(tmp_path)
        source_root = tmp_path / "recovered"
        source_dir = source_root / "pressure_door_1x1"
        source_dir.mkdir(parents=True)
        blend_path = source_dir / "pressure_door_1x1.blend"
        blend_path.write_bytes(b"blend fixture")
>       spec = validator.load_candidate_source_spec(
            tmp_path, "pressure_door_1x1", CANDIDATE_GLB_RELATIVE
        )

tests/test_validate_structural_sources.py:359: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_source_record_h0')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_source_record_h0/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_source_record_h0/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_____ test_candidate_requires_a_real_expected_blend_before_sidecar_passes ______

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_requires_a_real0')

    def test_candidate_requires_a_real_expected_blend_before_sidecar_passes(
        tmp_path: Path,
    ) -> None:
>       source_root, record_path, _record = _write_candidate_outputs(
            tmp_path, with_blend=False
        )

tests/test_validate_structural_sources.py:389: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_validate_structural_sources.py:198: in _write_candidate_outputs
    spec = validator.load_candidate_source_spec(
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_requires_a_real0')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_requires_a_real0/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_requires_a_real0/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_ test_candidate_validates_source_record_identity_and_placement_fields[kit_id-wrong-kit-source record kit_id does not match contract: pressure_door_1x1] _

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_validates_sourc0')
field = 'kit_id', value = 'wrong-kit'
expected = 'source record kit_id does not match contract: pressure_door_1x1'

    @pytest.mark.parametrize(
        "field, value, expected",
        [
            (
                "kit_id",
                "wrong-kit",
                "source record kit_id does not match contract: pressure_door_1x1",
            ),
            (
                "coordinate_conversion",
                "wrong-conversion",
                "source record coordinate_conversion does not match contract: pressure_door_1x1",
            ),
            (
                "placement_origin",
                "wrong-origin",
                "source record placement_origin does not match contract: pressure_door_1x1",
            ),
            (
                "blend_path",
                "/outside/pressure_door_1x1.blend",
                "source record blend_path does not match expected source: pressure_door_1x1",
            ),
        ],
    )
    def test_candidate_validates_source_record_identity_and_placement_fields(
        tmp_path: Path, field: str, value: object, expected: str
    ) -> None:
>       source_root, record_path, record = _write_candidate_outputs(tmp_path)

tests/test_validate_structural_sources.py:435: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_validate_structural_sources.py:198: in _write_candidate_outputs
    spec = validator.load_candidate_source_spec(
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_validates_sourc0')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_validates_sourc0/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_validates_sourc0/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_ test_candidate_validates_source_record_identity_and_placement_fields[coordinate_conversion-wrong-conversion-source record coordinate_conversion does not match contract: pressure_door_1x1] _

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_validates_sourc1')
field = 'coordinate_conversion', value = 'wrong-conversion'
expected = 'source record coordinate_conversion does not match contract: pressure_door_1x1'

    @pytest.mark.parametrize(
        "field, value, expected",
        [
            (
                "kit_id",
                "wrong-kit",
                "source record kit_id does not match contract: pressure_door_1x1",
            ),
            (
                "coordinate_conversion",
                "wrong-conversion",
                "source record coordinate_conversion does not match contract: pressure_door_1x1",
            ),
            (
                "placement_origin",
                "wrong-origin",
                "source record placement_origin does not match contract: pressure_door_1x1",
            ),
            (
                "blend_path",
                "/outside/pressure_door_1x1.blend",
                "source record blend_path does not match expected source: pressure_door_1x1",
            ),
        ],
    )
    def test_candidate_validates_source_record_identity_and_placement_fields(
        tmp_path: Path, field: str, value: object, expected: str
    ) -> None:
>       source_root, record_path, record = _write_candidate_outputs(tmp_path)

tests/test_validate_structural_sources.py:435: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_validate_structural_sources.py:198: in _write_candidate_outputs
    spec = validator.load_candidate_source_spec(
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_validates_sourc1')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_validates_sourc1/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_validates_sourc1/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_ test_candidate_validates_source_record_identity_and_placement_fields[placement_origin-wrong-origin-source record placement_origin does not match contract: pressure_door_1x1] _

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_validates_sourc2')
field = 'placement_origin', value = 'wrong-origin'
expected = 'source record placement_origin does not match contract: pressure_door_1x1'

    @pytest.mark.parametrize(
        "field, value, expected",
        [
            (
                "kit_id",
                "wrong-kit",
                "source record kit_id does not match contract: pressure_door_1x1",
            ),
            (
                "coordinate_conversion",
                "wrong-conversion",
                "source record coordinate_conversion does not match contract: pressure_door_1x1",
            ),
            (
                "placement_origin",
                "wrong-origin",
                "source record placement_origin does not match contract: pressure_door_1x1",
            ),
            (
                "blend_path",
                "/outside/pressure_door_1x1.blend",
                "source record blend_path does not match expected source: pressure_door_1x1",
            ),
        ],
    )
    def test_candidate_validates_source_record_identity_and_placement_fields(
        tmp_path: Path, field: str, value: object, expected: str
    ) -> None:
>       source_root, record_path, record = _write_candidate_outputs(tmp_path)

tests/test_validate_structural_sources.py:435: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_validate_structural_sources.py:198: in _write_candidate_outputs
    spec = validator.load_candidate_source_spec(
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_validates_sourc2')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_validates_sourc2/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_validates_sourc2/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_ test_candidate_validates_source_record_identity_and_placement_fields[blend_path-/outside/pressure_door_1x1.blend-source record blend_path does not match expected source: pressure_door_1x1] _

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_validates_sourc3')
field = 'blend_path', value = '/outside/pressure_door_1x1.blend'
expected = 'source record blend_path does not match expected source: pressure_door_1x1'

    @pytest.mark.parametrize(
        "field, value, expected",
        [
            (
                "kit_id",
                "wrong-kit",
                "source record kit_id does not match contract: pressure_door_1x1",
            ),
            (
                "coordinate_conversion",
                "wrong-conversion",
                "source record coordinate_conversion does not match contract: pressure_door_1x1",
            ),
            (
                "placement_origin",
                "wrong-origin",
                "source record placement_origin does not match contract: pressure_door_1x1",
            ),
            (
                "blend_path",
                "/outside/pressure_door_1x1.blend",
                "source record blend_path does not match expected source: pressure_door_1x1",
            ),
        ],
    )
    def test_candidate_validates_source_record_identity_and_placement_fields(
        tmp_path: Path, field: str, value: object, expected: str
    ) -> None:
>       source_root, record_path, record = _write_candidate_outputs(tmp_path)

tests/test_validate_structural_sources.py:435: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_validate_structural_sources.py:198: in _write_candidate_outputs
    spec = validator.load_candidate_source_spec(
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_validates_sourc3')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_validates_sourc3/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_validates_sourc3/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
________ test_candidate_validates_complete_socket_identity_and_metadata ________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_validates_compl0')

    def test_candidate_validates_complete_socket_identity_and_metadata(
        tmp_path: Path,
    ) -> None:
>       source_root, record_path, record = _write_candidate_outputs(tmp_path)

tests/test_validate_structural_sources.py:455: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_validate_structural_sources.py:198: in _write_candidate_outputs
    spec = validator.load_candidate_source_spec(
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_validates_compl0')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_validates_compl0/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_validates_compl0/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_________________ test_candidate_rejects_symlinked_blend_leaf __________________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_rejects_symlink0')

    def test_candidate_rejects_symlinked_blend_leaf(tmp_path: Path) -> None:
>       source_root, _record_path, _record = _write_candidate_outputs(tmp_path)

tests/test_validate_structural_sources.py:479: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_validate_structural_sources.py:198: in _write_candidate_outputs
    spec = validator.load_candidate_source_spec(
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_rejects_symlink0')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_rejects_symlink0/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_rejects_symlink0/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_____________ test_candidate_rejects_symlinked_source_record_leaf ______________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_rejects_symlink1')

    def test_candidate_rejects_symlinked_source_record_leaf(
        tmp_path: Path,
    ) -> None:
>       source_root, record_path, record = _write_candidate_outputs(tmp_path)

tests/test_validate_structural_sources.py:510: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_validate_structural_sources.py:198: in _write_candidate_outputs
    spec = validator.load_candidate_source_spec(
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_rejects_symlink1')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_rejects_symlink1/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_rejects_symlink1/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
______ test_candidate_source_record_symlink_loop_is_a_deterministic_error ______

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_source_record_s0')

    def test_candidate_source_record_symlink_loop_is_a_deterministic_error(
        tmp_path: Path,
    ) -> None:
>       source_root, record_path, _record = _write_candidate_outputs(tmp_path)

tests/test_validate_structural_sources.py:539: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_validate_structural_sources.py:198: in _write_candidate_outputs
    spec = validator.load_candidate_source_spec(
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_source_record_s0')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_source_record_s0/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_source_record_s0/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_ test_candidate_source_module_symlink_loop_is_a_deterministic_validation_error _

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_source_module_s0')

    def test_candidate_source_module_symlink_loop_is_a_deterministic_validation_error(
        tmp_path: Path,
    ) -> None:
        _write_candidate_project(tmp_path)
        source_root = tmp_path / "recovered"
        source_root.mkdir()
        module_root = source_root / "pressure_door_1x1"
        try:
            module_root.symlink_to(module_root.name, target_is_directory=True)
        except OSError as exc:
            pytest.skip(f"symlinks unavailable: {exc}")
        args = validator.parse_args(
            [
                "--project-root",
                str(tmp_path),
                "--source-root",
                str(source_root),
                "--candidate-source-glb="
                + "pressure_door_1x1="
                + str(CANDIDATE_GLB_RELATIVE),
            ]
        )
    
        errors = validate_sources(args)
    
        assert len(errors) == 1
>       assert "cannot resolve candidate source output path" in errors[0]
E       AssertionError: assert 'cannot resolve candidate source output path' in 'pressure_door_1x1: zip() takes no keyword arguments'

tests/test_validate_structural_sources.py:589: AssertionError
_______ test_candidate_cli_happy_path_emits_pass_without_running_blender _______

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_cli_happy_path_0')

    def test_candidate_cli_happy_path_emits_pass_without_running_blender(
        tmp_path: Path,
    ) -> None:
>       source_root, _record_path, _record = _write_candidate_outputs(tmp_path)

tests/test_validate_structural_sources.py:595: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_validate_structural_sources.py:198: in _write_candidate_outputs
    spec = validator.load_candidate_source_spec(
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_cli_happy_path_0')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_cli_happy_path_0/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_cli_happy_path_0/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
________ test_candidate_cli_rejects_symlinked_record_without_traceback _________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_cli_rejects_sym0')

    def test_candidate_cli_rejects_symlinked_record_without_traceback(
        tmp_path: Path,
    ) -> None:
>       source_root, record_path, record = _write_candidate_outputs(tmp_path)

tests/test_validate_structural_sources.py:612: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_validate_structural_sources.py:198: in _write_candidate_outputs
    spec = validator.load_candidate_source_spec(
tools/structural_source_contract.py:347: in load_candidate_source_spec
    return _load_spec_from_paths(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_cli_rejects_sym0')
module_id = 'pressure_door_1x1'
contract_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...est_candidate_cli_rejects_sym0/data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json')
source_glb_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-...st-27/test_candidate_cli_rejects_sym0/assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb')

    def _load_spec_from_paths(
        root: Path,
        module_id: str,
        contract_path: Path,
        source_glb_path: Path,
        *,
        missing_source_label: str = "missing source GLB",
    ) -> StructuralSourceSpec:
        resolved_contract_path = _resolve_path(contract_path, "structural contract path")
    
        if resolved_contract_path == root or root not in resolved_contract_path.parents:
            raise ValueError(f"structural contract path escapes project root: {module_id}")
        resolved_source_glb_path = _resolve_path(source_glb_path, "source GLB path")
        if resolved_source_glb_path == root or root not in resolved_source_glb_path.parents:
            raise ValueError(f"source GLB path escapes project root: {module_id}")
    
        try:
            contract_bytes = contract_path.read_bytes()
        except OSError as exc:
            raise ValueError(f"cannot read structural contract: {module_id}") from exc
        try:
            document = json.loads(contract_bytes.decode("utf-8"))
        except (UnicodeDecodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid structural contract JSON: {module_id}") from exc
        if not isinstance(document, dict):
            raise ValueError(f"invalid structural contract document: {module_id}")
    
        if document.get("document_kind") != "modular_asset_spec":
            raise ValueError(f"invalid structural contract kind: {module_id}")
        if document.get("module_id") != module_id:
            raise ValueError(f"contract module_id mismatch: {module_id}")
        if document.get("schema_version") != "1.0.0":
            raise ValueError(f"invalid structural contract schema_version: {module_id}")
        if document.get("asset_id") != module_id:
            raise ValueError(f"contract asset_id mismatch: {module_id}")
    
        kit_id = _nonempty_string(document.get("kit_id"), "kit_id")
        module_family = _nonempty_string(document.get("module_family"), "module_family")
        grid_step_m = _finite_float(document.get("grid_step_m"), "grid_step_m")
        if grid_step_m <= 0.0:
            raise ValueError("invalid structural contract grid_step_m")
    
        bounds = document.get("bounds")
        if not isinstance(bounds, dict):
            raise ValueError(f"invalid structural contract bounds: {module_id}")
        bounds_min_y_up = _coordinate_tuple(bounds.get("local_min_m"), "bounds.local_min_m")
        bounds_max_y_up = _coordinate_tuple(bounds.get("local_max_m"), "bounds.local_max_m")
        if any(
            minimum > maximum
>           for minimum, maximum in zip(bounds_min_y_up, bounds_max_y_up, strict=True)
        ):
E       TypeError: zip() takes no keyword arguments

tools/structural_source_contract.py:246: TypeError
_______ test_candidate_cli_rejects_source_module_loop_without_traceback ________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_candidate_cli_rejects_sou0')

    def test_candidate_cli_rejects_source_module_loop_without_traceback(
        tmp_path: Path,
    ) -> None:
        _write_candidate_project(tmp_path)
        source_root = tmp_path / "recovered"
        source_root.mkdir()
        module_root = source_root / "pressure_door_1x1"
        try:
            module_root.symlink_to(module_root.name, target_is_directory=True)
        except OSError as exc:
            pytest.skip(f"symlinks unavailable: {exc}")
    
        result = subprocess.run(
            _candidate_cli_args(tmp_path, source_root),
            capture_output=True,
            text=True,
            check=False,
        )
    
        assert result.returncode == 1
>       assert "cannot resolve candidate source output path" in result.stderr
E       AssertionError: assert 'cannot resolve candidate source output path' in 'ERROR: pressure_door_1x1: zip() takes no keyword arguments\n'
E        +  where 'ERROR: pressure_door_1x1: zip() takes no keyword arguments\n' = CompletedProcess(args=['/Applications/Xcode.app/Contents/Developer/usr/bin/python3', '/Volumes/Untitled/SynapticSeaAss...urce-only-validation'], returncode=1, stdout='', stderr='ERROR: pressure_door_1x1: zip() takes no keyword arguments\n').stderr

tests/test_validate_structural_sources.py:653: AssertionError
_ ValidateStructuralVariantBindingsTests.test_baseline_structural_fixture_is_accepted _

self = <test_validate_structural_variant_bindings.ValidateStructuralVariantBindingsTests testMethod=test_baseline_structural_fixture_is_accepted>

    def test_baseline_structural_fixture_is_accepted(self) -> None:
        with copied_structural_fixture() as project_root:
>           self.assertEqual([], run_structural_audit(project_root))
E           AssertionError: Lists differ: [] != ['damaged wrapper path does not match dama[965 chars]1x2']
E           
E           Second list contains 14 additional elements.
E           First extra element 0:
E           'damaged wrapper path does not match damaged variant: floor_1x1'
E           
E           Diff is 1059 characters long. Set self.maxDiff to None to see it.

tests/test_validate_structural_variant_bindings.py:138: AssertionError
_ ValidateStructuralVariantBindingsTests.test_non_variant_wrapper_is_ignored_when_it_has_no_integrity_triplet _

self = <test_validate_structural_variant_bindings.ValidateStructuralVariantBindingsTests testMethod=test_non_variant_wrapper_is_ignored_when_it_has_no_integrity_triplet>

    def test_non_variant_wrapper_is_ignored_when_it_has_no_integrity_triplet(self) -> None:
        with copied_non_variant_structural_fixture("wall_end_cap") as project_root:
>           self.assertEqual([], run_structural_audit(project_root))
E           AssertionError: Lists differ: [] != ['damaged wrapper path does not match dama[965 chars]1x2']
E           
E           Second list contains 14 additional elements.
E           First extra element 0:
E           'damaged wrapper path does not match damaged variant: floor_1x1'
E           
E           Diff is 1059 characters long. Set self.maxDiff to None to see it.

tests/test_validate_structural_variant_bindings.py:162: AssertionError
_ ValidateStructuralVariantBindingsTests.test_wrapper_path_that_differs_from_variant_path_is_rejected _

self = <test_validate_structural_variant_bindings.ValidateStructuralVariantBindingsTests testMethod=test_wrapper_path_that_differs_from_variant_path_is_rejected>

    def test_wrapper_path_that_differs_from_variant_path_is_rejected(self) -> None:
>       with copied_structural_fixture_with_wrapper_ref(
            "floor_1x1", "VisualInstance_Damaged", "floor_1x1.glb"
        ) as project_root:

tests/test_validate_structural_variant_bindings.py:145: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.9/lib/python3.9/contextlib.py:117: in __enter__
    return next(self.gen)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

asset_id = 'floor_1x1', node_name = 'VisualInstance_Damaged'
ref_name = 'floor_1x1.glb'

    @contextlib.contextmanager
    def copied_structural_fixture_with_wrapper_ref(
        asset_id: str, node_name: str, ref_name: str
    ) -> Iterator[Path]:
        with copied_structural_fixture() as root:
            scene_path = root / WRAPPER_ROOT / f"{asset_id}.tscn"
            scene = scene_path.read_text(encoding="utf-8")
            damaged_path = (
                f"res://{STRUCTURAL_ROOT.as_posix()}/{asset_id}/{asset_id}_damaged.glb"
            )
            replacement_path = (
                f"res://{STRUCTURAL_ROOT.as_posix()}/{asset_id}/{ref_name}"
            )
            marker = f'parent="Visual" instance=ExtResource("2_visual_damaged")'
            if node_name != "VisualInstance_Damaged":
                raise AssertionError(f"unsupported fixture node: {node_name}")
            if damaged_path not in scene or marker not in scene:
>               raise AssertionError("canonical damaged wrapper reference not found")
E               AssertionError: canonical damaged wrapper reference not found

tests/test_validate_structural_variant_bindings.py:86: AssertionError
_____ test_blender_owned_suffixed_library_remnant_is_repaired_canonically ______

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_owned_suffixed_li0')

    def test_blender_owned_suffixed_library_remnant_is_repaired_canonically(
        tmp_path: Path,
    ) -> None:
        for fixture in (BLENDER, SOURCE_FIXTURE, MATERIAL_FIXTURE):
            if not fixture.is_file():
                pytest.fail(f"fixture missing: {fixture}")
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / "floor_1x1"
        source_dir.mkdir(parents=True)
        source_blend = source_dir / "floor_1x1.blend"
        shutil.copy2(SOURCE_FIXTURE, source_blend)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
    
        seed_expr = (
            "import bpy; "
            f"bpy.ops.wm.open_mainfile(filepath={str(source_blend)!r}); "
            "canonical=bpy.data.materials['MAT_PaintedAlloyGray']; "
            "[setattr(slot, 'material', None) for obj in bpy.data.objects for slot in obj.material_slots if slot.material is canonical]; "
            "bpy.data.materials.remove(canonical); "
            f"bpy.ops.wm.save_as_mainfile(filepath={str(source_blend)!r})"
        )
        seeded = subprocess.run(
            [str(BLENDER), "--background", "--factory-startup", "--python-expr", seed_expr],
            capture_output=True,
            text=True,
            check=False,
            timeout=60,
        )
        assert seeded.returncode == 0, seeded.stdout + seeded.stderr
    
        command = _recipe_command(
            project_root=PROJECT_ROOT,
            structural_root=structural_root,
            props_root=props_root,
            asset_id="floor_1x1",
        )
        first = _recipe_with_external_material_library(command)
>       assert first.returncode == 0, first.stdout + first.stderr
E       AssertionError: 00:00.278  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_owned_suffixed_li0/structural/floor_1x1/floor_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: canonical material 'MAT_WarningStripe' is occupied by a non-owned material; refusing to rename, delete, or use it; one-time manual migration required
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...ipe' is occupied by a non-owned material; refusing to rename, delete, or use it; one-time manual migration required\n").returncode

tests/test_focused_nine_blender_recipes.py:834: AssertionError
_____ test_blender_verified_remnant_with_wrong_module_context_is_rejected ______

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_verified_remnant_0')

    def test_blender_verified_remnant_with_wrong_module_context_is_rejected(
        tmp_path: Path,
    ) -> None:
        for fixture in (BLENDER, SOURCE_FIXTURE, MATERIAL_FIXTURE):
            if not fixture.is_file():
                pytest.fail(f"fixture missing: {fixture}")
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / "floor_1x1"
        source_dir.mkdir(parents=True)
        source_blend = source_dir / "floor_1x1.blend"
        shutil.copy2(SOURCE_FIXTURE, source_blend)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
    
        seed_expr = (
            "import bpy; "
            f"bpy.ops.wm.open_mainfile(filepath={str(source_blend)!r}); "
            "canonical=bpy.data.materials['MAT_PaintedAlloyGray']; "
            "[setattr(slot, 'material', None) for obj in bpy.data.objects for slot in obj.material_slots if slot.material is canonical]; "
            "bpy.data.materials.remove(canonical); "
            "wrong_root=bpy.data.objects.new('ModuleRoot_wall_straight_1x1', None); "
            "bpy.context.scene.collection.objects.link(wrong_root); "
            "generated=bpy.data.objects['FocusedNine_floor_1x1_floor_panel']; generated.parent=wrong_root; "
            f"bpy.ops.wm.save_as_mainfile(filepath={str(source_blend)!r})"
        )
        seeded = subprocess.run(
            [str(BLENDER), "--background", "--factory-startup", "--python-expr", seed_expr],
            capture_output=True,
            text=True,
            check=False,
            timeout=60,
        )
        assert seeded.returncode == 0, seeded.stdout + seeded.stderr
        before = source_blend.read_bytes()
    
        result = _recipe_with_external_material_library(
            _recipe_command(
                project_root=PROJECT_ROOT,
                structural_root=structural_root,
                props_root=props_root,
                asset_id="floor_1x1",
            )
        )
        assert result.returncode == 1, result.stdout + result.stderr
>       assert "has an authored user" in result.stdout + result.stderr
E       assert 'has an authored user' in ('00:00.313  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_k...ied_remnant_0/structural/floor_1x1/floor_1x1.blend"\nBlender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)\n' + "ERROR: canonical material 'MAT_WarningStripe' is occupied by a non-owned material; refusing to rename, delete, or use it; one-time manual migration required\n")
E        +  where '00:00.313  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_k...ied_remnant_0/structural/floor_1x1/floor_1x1.blend"\nBlender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)\n' = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...ipe' is occupied by a non-owned material; refusing to rename, delete, or use it; one-time manual migration required\n").stdout
E        +  and   "ERROR: canonical material 'MAT_WarningStripe' is occupied by a non-owned material; refusing to rename, delete, or use it; one-time manual migration required\n" = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...ipe' is occupied by a non-owned material; refusing to rename, delete, or use it; one-time manual migration required\n").stderr

tests/test_focused_nine_blender_recipes.py:934: AssertionError
____ test_blender_owned_canonical_legacy_material_is_repaired_from_library _____

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_owned_canonical_l0')

    def test_blender_owned_canonical_legacy_material_is_repaired_from_library(
        tmp_path: Path,
    ) -> None:
        for fixture in (BLENDER, SOURCE_FIXTURE, MATERIAL_FIXTURE):
            if not fixture.is_file():
                pytest.fail(f"fixture missing: {fixture}")
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / "floor_1x1"
        source_dir.mkdir(parents=True)
        source_blend = source_dir / "floor_1x1.blend"
        shutil.copy2(SOURCE_FIXTURE, source_blend)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
    
        seed_expr = (
            "import bpy; "
            f"bpy.ops.wm.open_mainfile(filepath={str(source_blend)!r}); "
            "canonical=bpy.data.materials['MAT_PaintedAlloyGray']; "
            "[setattr(slot, 'material', None) for obj in bpy.data.objects for slot in obj.material_slots if slot.material is canonical]; "
            "bpy.data.materials.remove(canonical); "
            "legacy=bpy.data.materials['MAT_PaintedAlloyGray.001']; legacy.name='MAT_PaintedAlloyGray'; "
            "[legacy.__delitem__(key) for key in ('focused_nine_source_library', 'focused_nine_source_name') if key in legacy]; "
            f"bpy.ops.wm.save_as_mainfile(filepath={str(source_blend)!r})"
        )
        seeded = subprocess.run(
            [str(BLENDER), "--background", "--factory-startup", "--python-expr", seed_expr],
            capture_output=True,
            text=True,
            check=False,
            timeout=60,
        )
        assert seeded.returncode == 0, seeded.stdout + seeded.stderr
    
        result = _recipe_with_external_material_library(
            _recipe_command(
                project_root=PROJECT_ROOT,
                structural_root=structural_root,
                props_root=props_root,
                asset_id="floor_1x1",
            )
        )
>       assert result.returncode == 0, result.stdout + result.stderr
E       AssertionError: 00:00.290  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_owned_canonical_l0/structural/floor_1x1/floor_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: canonical material 'MAT_PaintedAlloyGray' is occupied by a non-owned material; refusing to rename, delete, or use it; one-time manual migration required
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...ray' is occupied by a non-owned material; refusing to rename, delete, or use it; one-time manual migration required\n").returncode

tests/test_focused_nine_blender_recipes.py:983: AssertionError
_______ test_blender_floor_recipe_emits_exact_material_names_and_exports _______

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_floor_recipe_emit0')

    def test_blender_floor_recipe_emits_exact_material_names_and_exports(tmp_path: Path) -> None:
        for fixture in (BLENDER, SOURCE_FIXTURE, MATERIAL_FIXTURE, CONTRACT_FIXTURE):
            if not fixture.is_file():
                pytest.fail(f"fixture missing: {fixture}")
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / "floor_1x1"
        source_dir.mkdir(parents=True)
        source_blend = source_dir / "floor_1x1.blend"
        shutil.copy2(SOURCE_FIXTURE, source_blend)
        _prepare_material_free_source(source_blend)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
    
        command = _recipe_command(
            project_root=PROJECT_ROOT,
            structural_root=structural_root,
            props_root=props_root,
            asset_id="floor_1x1",
        )
        first = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
>       assert first.returncode == 0, first.stdout + first.stderr
E       AssertionError: 00:00.290  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_floor_recipe_emit0/structural/floor_1x1/floor_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: generated object name 'FocusedNine_floor_1x1_floor_panel' is occupied; refusing to co-own an authored or stale object
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...nerated object name 'FocusedNine_floor_1x1_floor_panel' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:1043: AssertionError
____ test_blender_pressure_door_landmark_roles_are_distinct_and_idempotent _____

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_pressure_door_lan0')

    def test_blender_pressure_door_landmark_roles_are_distinct_and_idempotent(tmp_path: Path) -> None:
        for fixture in (
            BLENDER,
            PRESSURE_SOURCE_FIXTURE,
            PRESSURE_CONTRACT_FIXTURE,
            PRESSURE_CANDIDATE_CONTRACT_FIXTURE,
            PRESSURE_GLB_FIXTURE,
            MATERIAL_FIXTURE,
        ):
            if not fixture.is_file():
                pytest.fail(f"fixture missing: {fixture}")
        pressure_fixture_bytes = PRESSURE_SOURCE_FIXTURE.read_bytes()
        pressure_contract_bytes = PRESSURE_CONTRACT_FIXTURE.read_bytes()
        pressure_glb_bytes = PRESSURE_GLB_FIXTURE.read_bytes()
        material_fixture_bytes = MATERIAL_FIXTURE.read_bytes()
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / "pressure_door_1x1"
        source_dir.mkdir(parents=True)
        source_blend = source_dir / "pressure_door_1x1.blend"
        shutil.copy2(PRESSURE_SOURCE_FIXTURE, source_blend)
        _prepare_material_free_source(source_blend)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
    
        pressure_project = tmp_path / "pressure-project"
        pressure_contract = pressure_project / (
            "data/placement/contracts/structural/ship_structural_v0/pressure_door_1x1_contract.json"
        )
        pressure_glb = pressure_project / (
            "assets/_staging/focused_nine/structural/pressure_door_1x1/pressure_door_1x1.glb"
        )
        pressure_contract.parent.mkdir(parents=True)
        pressure_glb.parent.mkdir(parents=True)
        shutil.copy2(PRESSURE_CANDIDATE_CONTRACT_FIXTURE, pressure_contract)
        shutil.copy2(PRESSURE_GLB_FIXTURE, pressure_glb)
        prepare_pressure_source_expr = f"""
    import bpy
    bpy.ops.wm.open_mainfile(filepath={str(source_blend)!r})
    root = bpy.data.objects.get('ModuleRoot_doorway_frame_open_1x1')
    assert root is not None
    root.name = 'ModuleRoot_pressure_door_1x1'
    helpers = bpy.data.collections.get('AuthoringHelpers')
    assert helpers is not None
    legacy_rules = {{
        'intact': 'all pressure-door panels',
        'damaged': 'one cosmetic indicator panel omitted',
        'breached': 'central leaf omitted',
    }}
    for role, rule in legacy_rules.items():
        collection = bpy.data.collections.get('Export_' + role)
        if collection is None:
            collection = bpy.data.collections.new('Export_' + role)
            helpers.children.link(collection)
        collection['module_id'] = 'pressure_door_1x1'
        collection['variant_role'] = role
        collection['variant_visual_rule'] = rule
        collection['focused_nine_generated'] = True
        collection['focused_nine_asset_id'] = 'pressure_door_1x1'
    bpy.ops.wm.save_as_mainfile(filepath={str(source_blend)!r})
    """
        prepared = subprocess.run(
            [str(BLENDER), "--background", "--factory-startup", "--python-expr", prepare_pressure_source_expr],
            capture_output=True,
            text=True,
            check=False,
            timeout=60,
        )
        assert prepared.returncode == 0, prepared.stdout + prepared.stderr
    
        command = _recipe_command(
            project_root=pressure_project,
            structural_root=structural_root,
            props_root=props_root,
            asset_id="pressure_door_1x1",
        )
        first = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
        assert first.returncode == 0, first.stdout + first.stderr
        first_report, first_line = _report_from_stdout(first.stdout)
        second = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
        assert second.returncode == 0, second.stdout + second.stderr
        second_report, second_line = _report_from_stdout(second.stdout)
    
        assert first_report == second_report
        assert first_line == second_line
        prefix = "FocusedNine_pressure_door_1x1_"
        names = first_report["generated_object_names"]
        assert names
        assert all(name.startswith(prefix) for name in names)
        assert first_report["generated_count"] == len(names)
        for token in (
            "shared_frame_left",
            "shared_frame_right",
            "shared_rail_left",
            "shared_rail_right",
            "seal_left",
            "seal_right",
            "intact_lock_bar",
            "damaged_reinforcement",
            "breached_void_marker",
        ):
            assert any(token in name for name in names), (token, names)
        assert first_report["helper_names"] == [
            "Export_breached",
            "Export_damaged",
            "Export_intact",
        ]
        assert first_report["variant_visibility_policy"] == "source_visible_wrapper_authority"
        assert first_report["export_collection_visibility"] == {
            "intact": {"hide_viewport": False, "hide_render": False},
            "damaged": {"hide_viewport": False, "hide_render": False},
            "breached": {"hide_viewport": False, "hide_render": False},
        }
        damaged = {name for name in names if name.startswith(prefix + "damaged_")}
        breached = {name for name in names if name.startswith(prefix + "breached_")}
        assert damaged and breached and damaged != breached
        assert not any(name.endswith("_damaged_cyan_indicator_right") for name in names)
        assert not any(name.endswith("_breached_split_leaf_left") for name in names)
        assert any(name.endswith("_cyan_indicator_right") for name in names)
        assert any(name.endswith("_split_leaf_left") for name in names)
        assert first_report["boolean_modifiers"] == []
        assert "BOOLEAN" not in first_report["modifier_types"]
        assert first_report["triangle_count"] > 0
    
        role_probe = """
    import bpy
    import json
    bpy.ops.wm.open_mainfile(filepath={source!r})
    roles = {{
        name: (bpy.data.objects[name].get('focused_nine_role'), bpy.data.objects[name].get('landmark_role'))
        for name in (
            'FocusedNine_pressure_door_1x1_shared_frame_left',
            'FocusedNine_pressure_door_1x1_shared_rail_left',
            'FocusedNine_pressure_door_1x1_seal_left',
            'FocusedNine_pressure_door_1x1_intact_lock_bar',
        )
    }}
    print('PRESSURE_ROLE_PROOF '+json.dumps(roles, sort_keys=True))
    """.format(source=str(source_blend))
        role_result = subprocess.run(
            [str(BLENDER), "--background", "--factory-startup", "--python-expr", role_probe],
            capture_output=True,
            text=True,
            check=False,
            timeout=60,
        )
        assert role_result.returncode == 0, role_result.stdout + role_result.stderr
        role_line = next(
            line for line in role_result.stdout.splitlines() if line.startswith("PRESSURE_ROLE_PROOF ")
        )
        assert json.loads(role_line.removeprefix("PRESSURE_ROLE_PROOF ")) == {
            "FocusedNine_pressure_door_1x1_intact_lock_bar": ["Intact", "Intact"],
            "FocusedNine_pressure_door_1x1_seal_left": ["seal", "seal"],
            "FocusedNine_pressure_door_1x1_shared_frame_left": ["shared_frame", "shared_frame"],
            "FocusedNine_pressure_door_1x1_shared_rail_left": ["shared_rail", "shared_rail"],
        }
        assert PRESSURE_SOURCE_FIXTURE.read_bytes() == pressure_fixture_bytes
        assert PRESSURE_CONTRACT_FIXTURE.read_bytes() == pressure_contract_bytes
        assert PRESSURE_GLB_FIXTURE.read_bytes() == pressure_glb_bytes
        assert MATERIAL_FIXTURE.read_bytes() == material_fixture_bytes
    
>       exported = _export_structural_variants_for_evidence(
            source=source_blend,
            asset_id="pressure_door_1x1",
            destination=tmp_path / "exports",
        )

tests/test_focused_nine_blender_recipes.py:1237: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

    def _export_structural_variants_for_evidence(
        *, source: Path, asset_id: str, destination: Path
    ) -> dict[str, Path]:
        destination.mkdir(parents=True, exist_ok=True)
        command = [
            str(BLENDER),
            "--background",
            "--factory-startup",
            "--python",
            str(PROJECT_ROOT / "tools/export_structural_glb.py"),
            "--",
            "--blend-path",
            str(source),
            "--staging-dir",
            str(destination),
            "--module",
            asset_id,
        ]
        result = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
>       assert result.returncode == 0, result.stdout + result.stderr
E       AssertionError: 00:00.261  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_pressure_door_lan0/structural/pressure_door_1x1/pressure_door_1x1.blend"
E         INFO Draco is available, use library at /Applications/Blender.app/Contents/Resources/5.2/scripts/addons_core/io_scene_gltf2/libbf_intern_draco_bridge.dylib
E         INFO MeshOptimizer is available, use library at /Applications/Blender.app/Contents/Resources/5.2/scripts/addons_core/io_scene_gltf2/libbf_intern_meshopt_bridge.dylib
E         16:45:13 | INFO: Starting glTF 2.0 export
E         16:45:13 | INFO: Extracting primitive: Cube.064
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.066
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.065
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.052
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.050
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.045
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.047
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.046
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.058
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.059
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.054
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.055
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.056
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.057
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.049
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.051
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Finished glTF 2.0 export in 0.012488842010498047 s
E         
E         16:45:13 | INFO: Starting glTF 2.0 export
E         16:45:13 | INFO: Extracting primitive: Cube.052
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.063
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.062
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.061
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.050
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.045
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.047
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.046
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.058
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.059
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.054
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.055
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.056
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.057
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.049
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.051
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Finished glTF 2.0 export in 0.010011911392211914 s
E         
E         16:45:13 | INFO: Starting glTF 2.0 export
E         16:45:13 | INFO: Extracting primitive: Cube.052
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.053
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.060
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.050
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.045
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.047
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.046
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.058
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.059
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.054
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.055
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.056
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.057
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.048
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.049
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Extracting primitive: Cube.051
E         16:45:13 | INFO: Primitives created: 1
E         16:45:13 | INFO: Finished glTF 2.0 export in 0.009369134902954102 s
E         
E         16:45:13 | INFO: Data are loaded, start creating Blender stuff
E         16:45:13 | INFO: Blender create Mesh node Cube.064
E         16:45:13 | INFO: Blender create Mesh node Cube.066
E         16:45:13 | INFO: Blender create Mesh node Cube.065
E         16:45:13 | INFO: Blender create Mesh node Cube.052
E         16:45:13 | INFO: Blender create Mesh node Cube.050
E         16:45:13 | INFO: Blender create Mesh node Cube.045
E         16:45:13 | INFO: Blender create Mesh node Cube.047
E         16:45:13 | INFO: Blender create Mesh node Cube.046
E         16:45:13 | INFO: Blender create Mesh node Cube.058
E         16:45:13 | INFO: Blender create Mesh node Cube.059
E         16:45:13 | INFO: Blender create Mesh node Cube.054
E         16:45:13 | INFO: Blender create Mesh node Cube.055
E         16:45:13 | INFO: Blender create Mesh node Cube.056
E         16:45:13 | INFO: Blender create Mesh node Cube.057
E         16:45:13 | INFO: Blender create Mesh node Cube.049
E         16:45:13 | INFO: Blender create Mesh node Cube.051
E         16:45:13 | INFO: glTF import finished in 0.01s
E         16:45:13 | INFO: Data are loaded, start creating Blender stuff
E         16:45:13 | INFO: Blender create Mesh node Cube.052
E         16:45:13 | INFO: Blender create Mesh node Cube.063
E         16:45:13 | INFO: Blender create Mesh node Cube.062
E         16:45:13 | INFO: Blender create Mesh node Cube.061
E         16:45:13 | INFO: Blender create Mesh node Cube.050
E         16:45:13 | INFO: Blender create Mesh node Cube.045
E         16:45:13 | INFO: Blender create Mesh node Cube.047
E         16:45:13 | INFO: Blender create Mesh node Cube.046
E         16:45:13 | INFO: Blender create Mesh node Cube.058
E         16:45:13 | INFO: Blender create Mesh node Cube.059
E         16:45:13 | INFO: Blender create Mesh node Cube.054
E         16:45:13 | INFO: Blender create Mesh node Cube.055
E         16:45:13 | INFO: Blender create Mesh node Cube.056
E         16:45:13 | INFO: Blender create Mesh node Cube.057
E         16:45:13 | INFO: Blender create Mesh node Cube.049
E         16:45:13 | INFO: Blender create Mesh node Cube.051
E         16:45:13 | INFO: glTF import finished in 0.00s
E         16:45:13 | INFO: Data are loaded, start creating Blender stuff
E         16:45:13 | INFO: Blender create Mesh node Cube.052
E         16:45:13 | INFO: Blender create Mesh node Cube.053
E         16:45:13 | INFO: Blender create Mesh node Cube.060
E         16:45:13 | INFO: Blender create Mesh node Cube.050
E         16:45:13 | INFO: Blender create Mesh node Cube.045
E         16:45:13 | INFO: Blender create Mesh node Cube.047
E         16:45:13 | INFO: Blender create Mesh node Cube.046
E         16:45:13 | INFO: Blender create Mesh node Cube.058
E         16:45:13 | INFO: Blender create Mesh node Cube.059
E         16:45:13 | INFO: Blender create Mesh node Cube.054
E         16:45:13 | INFO: Blender create Mesh node Cube.055
E         16:45:13 | INFO: Blender create Mesh node Cube.056
E         16:45:13 | INFO: Blender create Mesh node Cube.057
E         16:45:13 | INFO: Blender create Mesh node Cube.048
E         16:45:13 | INFO: Blender create Mesh node Cube.049
E         16:45:13 | INFO: Blender create Mesh node Cube.051
E         16:45:13 | INFO: glTF import finished in 0.01s
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: one or more structural variants failed validation for pressure_door_1x1: fresh Blender GLB validation failed for pressure_door_1x1: unsupported geometry profile: pressure_door_1x1; fresh Blender GLB validation failed for pressure_door_1x1: unsupported geometry profile: pressure_door_1x1; fresh Blender GLB validation failed for pressure_door_1x1: unsupported geometry profile: pressure_door_1x1
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...oor_1x1; fresh Blender GLB validation failed for pressure_door_1x1: unsupported geometry profile: pressure_door_1x1\n').returncode

tests/test_focused_nine_blender_recipes.py:1755: AssertionError
__________ test_blender_floor_recipe_is_idempotent_and_source_scoped ___________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_floor_recipe_is_i0')

    def test_blender_floor_recipe_is_idempotent_and_source_scoped(tmp_path: Path) -> None:
        if not BLENDER.is_file():
            pytest.fail(f"Blender 5.2 executable missing: {BLENDER}")
        if not SOURCE_FIXTURE.is_file():
            pytest.fail(f"source fixture missing: {SOURCE_FIXTURE}")
        if not MATERIAL_FIXTURE.is_file():
            pytest.fail(f"material fixture missing: {MATERIAL_FIXTURE}")
        source_fixture_bytes = SOURCE_FIXTURE.read_bytes()
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / "floor_1x1"
        source_dir.mkdir(parents=True)
        source_blend = source_dir / "floor_1x1.blend"
        shutil.copy2(SOURCE_FIXTURE, source_blend)
        _prepare_material_free_source(source_blend)
        material_dir = structural_root.parent / "materials"
        material_dir.mkdir(parents=True)
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
        props_root = tmp_path / "props"
        props_root.mkdir()
    
        seed_expr = (
            "import bpy; "
            f"bpy.ops.wm.open_mainfile(filepath={str(source_blend)!r}); "
            "geometry=bpy.data.collections.get('Geometry') or bpy.data.collections.new('Geometry'); "
            "bpy.context.scene.collection.children.link(geometry) if geometry.name not in {child.name for child in bpy.context.scene.collection.children} else None; "
            "authored=bpy.data.objects.new('FocusedNine_floor_1x1_authored_panel', None); geometry.objects.link(authored); "
            "authored['authored_marker']='preserve'; "
            "stale=bpy.data.objects.new('FocusedNine_floor_1x1_stale_generated', None); geometry.objects.link(stale); "
            "stale['focused_nine_generated']=True; stale['focused_nine_asset_id']='floor_1x1'; "
            f"bpy.ops.wm.save_as_mainfile(filepath={str(source_blend)!r})"
        )
        prepared = subprocess.run(
            [str(BLENDER), "--background", "--factory-startup", "--python-expr", seed_expr],
            capture_output=True,
            text=True,
            check=False,
            timeout=60,
        )
        assert prepared.returncode == 0, prepared.stdout + prepared.stderr
    
        command = [
            str(BLENDER),
            "--background",
            "--factory-startup",
            "--python",
            str(PROJECT_ROOT / "tools/focused_nine_blender_recipes.py"),
            "--",
            "--project-root",
            str(PROJECT_ROOT),
            "--structural-source-root",
            str(structural_root),
            "--props-source-root",
            str(props_root),
            "--asset-id",
            "floor_1x1",
            "--overwrite-generated-only",
        ]
        first = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
>       assert first.returncode == 0, first.stdout + first.stderr
E       AssertionError: 00:00.307  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_floor_recipe_is_i0/structural/floor_1x1/floor_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: generated object name 'FocusedNine_floor_1x1_floor_panel' is occupied; refusing to co-own an authored or stale object
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...nerated object name 'FocusedNine_floor_1x1_floor_panel' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:1559: AssertionError
_______ test_blender_compatibility_wrapper_normalizes_blender_arguments ________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_compatibility_wra0')

    def test_blender_compatibility_wrapper_normalizes_blender_arguments(tmp_path: Path) -> None:
        if not BLENDER.is_file():
            pytest.fail(f"Blender 5.2 executable missing: {BLENDER}")
        if not SOURCE_FIXTURE.is_file():
            pytest.fail(f"source fixture missing: {SOURCE_FIXTURE}")
        if not MATERIAL_FIXTURE.is_file():
            pytest.fail(f"material fixture missing: {MATERIAL_FIXTURE}")
        if not CONTRACT_FIXTURE.is_file():
            pytest.fail(f"contract fixture missing: {CONTRACT_FIXTURE}")
        if not SOURCE_GLB_FIXTURE.is_file():
            pytest.fail(f"source GLB fixture missing: {SOURCE_GLB_FIXTURE}")
    
        project_root = tmp_path / "project"
        contract_copy = project_root / (
            "data/placement/contracts/structural/ship_structural_v0/floor_1x1_contract.json"
        )
        glb_copy = project_root / (
            "assets/imported/structural/ship_structural_v0/floor_1x1/floor_1x1.glb"
        )
        contract_copy.parent.mkdir(parents=True)
        glb_copy.parent.mkdir(parents=True)
        shutil.copy2(CONTRACT_FIXTURE, contract_copy)
        shutil.copy2(SOURCE_GLB_FIXTURE, glb_copy)
        contract_fixture_bytes = CONTRACT_FIXTURE.read_bytes()
        source_glb_fixture_bytes = SOURCE_GLB_FIXTURE.read_bytes()
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / "floor_1x1"
        source_dir.mkdir(parents=True)
        source_blend = source_dir / "floor_1x1.blend"
        shutil.copy2(SOURCE_FIXTURE, source_blend)
        _prepare_material_free_source(source_blend)
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        material_copy = material_dir / "salvage_industrial.blend"
        shutil.copy2(MATERIAL_FIXTURE, material_copy)
    
        source_fixture_bytes = SOURCE_FIXTURE.read_bytes()
        material_fixture_bytes = MATERIAL_FIXTURE.read_bytes()
        command = [
            str(BLENDER),
            "--background",
            "--factory-startup",
            "--python",
            str(PROJECT_ROOT / "tools/improve_floor_geometry.py"),
            "--",
            "--project-root",
            str(project_root),
            "--source-root",
            str(structural_root),
            "--module",
            "floor_1x1",
            "--overwrite",
        ]
        result = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
    
>       assert result.returncode == 0, result.stdout + result.stderr
E       AssertionError: 00:00.258  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_compatibility_wra0/structural/floor_1x1/floor_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: generated object name 'FocusedNine_floor_1x1_floor_panel' is occupied; refusing to co-own an authored or stale object
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...nerated object name 'FocusedNine_floor_1x1_floor_panel' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:1666: AssertionError
_______ test_blender_under_budget_recipes_meet_gameplay_evidence_minima ________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_under_budget_reci0')

    def test_blender_under_budget_recipes_meet_gameplay_evidence_minima(tmp_path: Path) -> None:
        from tools import focused_nine_evidence
    
        for fixture in (BLENDER, MATERIAL_FIXTURE, *(item[1] for item in UNDER_BUDGET_FIXTURES.values())):
            if not fixture.is_file():
                pytest.fail(f"fixture missing: {fixture}")
    
        structural_root = tmp_path / "structural"
        props_root = tmp_path / "props"
        material_dir = tmp_path / "materials"
        props_root.mkdir(parents=True)
        material_dir.mkdir(parents=True)
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
        fixture_bytes = {asset_id: fixture.read_bytes() for asset_id, (_, fixture, _) in UNDER_BUDGET_FIXTURES.items()}
    
        measured: dict[str, int] = {}
        for asset_id, (kind, fixture, minimum) in UNDER_BUDGET_FIXTURES.items():
            if kind == "structural":
                source_dir = structural_root / asset_id
                source_dir.mkdir(parents=True)
                source = source_dir / f"{asset_id}.blend"
            else:
                props_root.mkdir(parents=True, exist_ok=True)
                source = props_root / f"{asset_id}.blend"
            shutil.copy2(fixture, source)
            _prepare_material_free_source(source)
    
            command = _recipe_command(
                project_root=PROJECT_ROOT,
                structural_root=structural_root,
                props_root=props_root,
                asset_id=asset_id,
            )
            recipe = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
>           assert recipe.returncode == 0, recipe.stdout + recipe.stderr
E           AssertionError: 00:00.280  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_under_budget_reci0/structural/wall_straight_1x1/wall_straight_1x1.blend"
E             Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E             ERROR: generated object name 'FocusedNine_wall_straight_1x1_perimeter_left' is occupied; refusing to co-own an authored or stale object
E             
E           assert 1 == 0
E            +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...ect name 'FocusedNine_wall_straight_1x1_perimeter_left' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:1822: AssertionError
_ test_salvage_detail_recipes_are_named_canonical_and_contract_preserving[floor_1x1] _

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_salvage_detail_recipes_ar0')
asset_id = 'floor_1x1'

    @pytest.mark.parametrize("asset_id", tuple(SALVAGE_DETAIL_FIXTURES))
    def test_salvage_detail_recipes_are_named_canonical_and_contract_preserving(
        tmp_path: Path, asset_id: str
    ) -> None:
        from tools.focused_nine_blender_recipes import REQUIRED_MATERIAL_NAMES
    
        for fixture in (BLENDER, MATERIAL_FIXTURE, SALVAGE_DETAIL_FIXTURES[asset_id]):
            if not fixture.is_file():
                pytest.fail(f"fixture missing: {fixture}")
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / asset_id
        source_dir.mkdir(parents=True)
        source = source_dir / f"{asset_id}.blend"
        shutil.copy2(SALVAGE_DETAIL_FIXTURES[asset_id], source)
        _prepare_material_free_source(source)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
        before_identity = _structural_contract_identity(source, asset_id)
    
        command = _recipe_command(
            project_root=PROJECT_ROOT,
            structural_root=structural_root,
            props_root=props_root,
            asset_id=asset_id,
        )
        first = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
>       assert first.returncode == 0, first.stdout + first.stderr
E       AssertionError: 00:00.283  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_salvage_detail_recipes_ar0/structural/floor_1x1/floor_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: generated object name 'FocusedNine_floor_1x1_floor_panel' is occupied; refusing to co-own an authored or stale object
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...nerated object name 'FocusedNine_floor_1x1_floor_panel' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:1946: AssertionError
_ test_salvage_detail_recipes_are_named_canonical_and_contract_preserving[wall_straight_1x1] _

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_salvage_detail_recipes_ar1')
asset_id = 'wall_straight_1x1'

    @pytest.mark.parametrize("asset_id", tuple(SALVAGE_DETAIL_FIXTURES))
    def test_salvage_detail_recipes_are_named_canonical_and_contract_preserving(
        tmp_path: Path, asset_id: str
    ) -> None:
        from tools.focused_nine_blender_recipes import REQUIRED_MATERIAL_NAMES
    
        for fixture in (BLENDER, MATERIAL_FIXTURE, SALVAGE_DETAIL_FIXTURES[asset_id]):
            if not fixture.is_file():
                pytest.fail(f"fixture missing: {fixture}")
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / asset_id
        source_dir.mkdir(parents=True)
        source = source_dir / f"{asset_id}.blend"
        shutil.copy2(SALVAGE_DETAIL_FIXTURES[asset_id], source)
        _prepare_material_free_source(source)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
        before_identity = _structural_contract_identity(source, asset_id)
    
        command = _recipe_command(
            project_root=PROJECT_ROOT,
            structural_root=structural_root,
            props_root=props_root,
            asset_id=asset_id,
        )
        first = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
>       assert first.returncode == 0, first.stdout + first.stderr
E       AssertionError: 00:00.294  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_salvage_detail_recipes_ar1/structural/wall_straight_1x1/wall_straight_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: generated object name 'FocusedNine_wall_straight_1x1_perimeter_left' is occupied; refusing to co-own an authored or stale object
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...ect name 'FocusedNine_wall_straight_1x1_perimeter_left' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:1946: AssertionError
_ test_salvage_detail_recipes_are_named_canonical_and_contract_preserving[ramp_up_1x2] _

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_salvage_detail_recipes_ar2')
asset_id = 'ramp_up_1x2'

    @pytest.mark.parametrize("asset_id", tuple(SALVAGE_DETAIL_FIXTURES))
    def test_salvage_detail_recipes_are_named_canonical_and_contract_preserving(
        tmp_path: Path, asset_id: str
    ) -> None:
        from tools.focused_nine_blender_recipes import REQUIRED_MATERIAL_NAMES
    
        for fixture in (BLENDER, MATERIAL_FIXTURE, SALVAGE_DETAIL_FIXTURES[asset_id]):
            if not fixture.is_file():
                pytest.fail(f"fixture missing: {fixture}")
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / asset_id
        source_dir.mkdir(parents=True)
        source = source_dir / f"{asset_id}.blend"
        shutil.copy2(SALVAGE_DETAIL_FIXTURES[asset_id], source)
        _prepare_material_free_source(source)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
        before_identity = _structural_contract_identity(source, asset_id)
    
        command = _recipe_command(
            project_root=PROJECT_ROOT,
            structural_root=structural_root,
            props_root=props_root,
            asset_id=asset_id,
        )
        first = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
>       assert first.returncode == 0, first.stdout + first.stderr
E       AssertionError: 00:00.292  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_salvage_detail_recipes_ar2/structural/ramp_up_1x2/ramp_up_1x2.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: generated object name 'FocusedNine_ramp_up_1x2_tread_00' is occupied; refusing to co-own an authored or stale object
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...enerated object name 'FocusedNine_ramp_up_1x2_tread_00' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:1946: AssertionError
_ test_salvage_detail_recipes_are_named_canonical_and_contract_preserving[pillar_support_1x1] _

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_salvage_detail_recipes_ar3')
asset_id = 'pillar_support_1x1'

    @pytest.mark.parametrize("asset_id", tuple(SALVAGE_DETAIL_FIXTURES))
    def test_salvage_detail_recipes_are_named_canonical_and_contract_preserving(
        tmp_path: Path, asset_id: str
    ) -> None:
        from tools.focused_nine_blender_recipes import REQUIRED_MATERIAL_NAMES
    
        for fixture in (BLENDER, MATERIAL_FIXTURE, SALVAGE_DETAIL_FIXTURES[asset_id]):
            if not fixture.is_file():
                pytest.fail(f"fixture missing: {fixture}")
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / asset_id
        source_dir.mkdir(parents=True)
        source = source_dir / f"{asset_id}.blend"
        shutil.copy2(SALVAGE_DETAIL_FIXTURES[asset_id], source)
        _prepare_material_free_source(source)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
        before_identity = _structural_contract_identity(source, asset_id)
    
        command = _recipe_command(
            project_root=PROJECT_ROOT,
            structural_root=structural_root,
            props_root=props_root,
            asset_id=asset_id,
        )
        first = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
>       assert first.returncode == 0, first.stdout + first.stderr
E       AssertionError: 00:00.302  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_salvage_detail_recipes_ar3/structural/pillar_support_1x1/pillar_support_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: generated object name 'FocusedNine_pillar_support_1x1_pillar_body' is occupied; refusing to co-own an authored or stale object
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...bject name 'FocusedNine_pillar_support_1x1_pillar_body' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:1946: AssertionError
_ test_salvage_detail_recipes_are_named_canonical_and_contract_preserving[ceiling_cap_1x1] _

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_salvage_detail_recipes_ar4')
asset_id = 'ceiling_cap_1x1'

    @pytest.mark.parametrize("asset_id", tuple(SALVAGE_DETAIL_FIXTURES))
    def test_salvage_detail_recipes_are_named_canonical_and_contract_preserving(
        tmp_path: Path, asset_id: str
    ) -> None:
        from tools.focused_nine_blender_recipes import REQUIRED_MATERIAL_NAMES
    
        for fixture in (BLENDER, MATERIAL_FIXTURE, SALVAGE_DETAIL_FIXTURES[asset_id]):
            if not fixture.is_file():
                pytest.fail(f"fixture missing: {fixture}")
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / asset_id
        source_dir.mkdir(parents=True)
        source = source_dir / f"{asset_id}.blend"
        shutil.copy2(SALVAGE_DETAIL_FIXTURES[asset_id], source)
        _prepare_material_free_source(source)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
        before_identity = _structural_contract_identity(source, asset_id)
    
        command = _recipe_command(
            project_root=PROJECT_ROOT,
            structural_root=structural_root,
            props_root=props_root,
            asset_id=asset_id,
        )
        first = subprocess.run(command, capture_output=True, text=True, check=False, timeout=60)
>       assert first.returncode == 0, first.stdout + first.stderr
E       AssertionError: 00:00.289  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_salvage_detail_recipes_ar4/structural/ceiling_cap_1x1/ceiling_cap_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: generated object name 'FocusedNine_ceiling_cap_1x1_ceiling_cap' is occupied; refusing to co-own an authored or stale object
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...d object name 'FocusedNine_ceiling_cap_1x1_ceiling_cap' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:1946: AssertionError
_________ test_blender_pillar_details_overlap_body_by_real_world_aabb __________

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_pillar_details_ov0')

    def test_blender_pillar_details_overlap_body_by_real_world_aabb(tmp_path: Path) -> None:
        fixture = SALVAGE_DETAIL_FIXTURES["pillar_support_1x1"]
        for candidate in (BLENDER, fixture, MATERIAL_FIXTURE):
            if not candidate.is_file():
                pytest.fail(f"fixture missing: {candidate}")
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / "pillar_support_1x1"
        source_dir.mkdir(parents=True)
        source = source_dir / "pillar_support_1x1.blend"
        shutil.copy2(fixture, source)
        _prepare_material_free_source(source)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
    
        result = _recipe_with_external_material_library(
            _recipe_command(
                project_root=PROJECT_ROOT,
                structural_root=structural_root,
                props_root=props_root,
                asset_id="pillar_support_1x1",
            )
        )
>       assert result.returncode == 0, result.stdout + result.stderr
E       AssertionError: 00:00.286  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_pillar_details_ov0/structural/pillar_support_1x1/pillar_support_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: generated object name 'FocusedNine_pillar_support_1x1_pillar_body' is occupied; refusing to co-own an authored or stale object
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...bject name 'FocusedNine_pillar_support_1x1_pillar_body' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:2096: AssertionError
______ test_blender_wall_frame_is_four_rails_without_panel_face_occlusion ______

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_wall_frame_is_fou0')

    def test_blender_wall_frame_is_four_rails_without_panel_face_occlusion(
        tmp_path: Path,
    ) -> None:
        fixture = SALVAGE_DETAIL_FIXTURES["wall_straight_1x1"]
        for candidate in (BLENDER, fixture, MATERIAL_FIXTURE):
            if not candidate.is_file():
                pytest.fail(f"fixture missing: {candidate}")
    
        structural_root = tmp_path / "structural"
        source_dir = structural_root / "wall_straight_1x1"
        source_dir.mkdir(parents=True)
        source = source_dir / "wall_straight_1x1.blend"
        shutil.copy2(fixture, source)
        _prepare_material_free_source(source)
        props_root = tmp_path / "props"
        props_root.mkdir()
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
    
        result = _recipe_with_external_material_library(
            _recipe_command(
                project_root=PROJECT_ROOT,
                structural_root=structural_root,
                props_root=props_root,
                asset_id="wall_straight_1x1",
            )
        )
>       assert result.returncode == 0, result.stdout + result.stderr
E       AssertionError: 00:00.289  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_blender_wall_frame_is_fou0/structural/wall_straight_1x1/wall_straight_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: generated object name 'FocusedNine_wall_straight_1x1_perimeter_left' is occupied; refusing to co-own an authored or stale object
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...ect name 'FocusedNine_wall_straight_1x1_perimeter_left' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:2160: AssertionError
_ test_landmark_recipes_emit_deterministic_role_inventories_and_canonical_materials[doorway_frame_open_1x1-fixture0-structural-tokens0] _

tmp_path = PosixPath('/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_landmark_recipes_emit_det0')
asset_id = 'doorway_frame_open_1x1'
fixture = PosixPath('/Volumes/Untitled/SynapticSeaAssets/meshes/source/ship_structural_v0/doorway_frame_open_1x1/doorway_frame_open_1x1.blend')
kind = 'structural'
tokens = ('frame_inner_left', 'frame_inner_right', 'mechanical_rail_left', 'mechanical_rail_right', 'seal_left', 'seal_right', ...)

    @pytest.mark.parametrize(
        ("asset_id", "fixture", "kind", "tokens"),
        [
            (
                "doorway_frame_open_1x1",
                Path(
                    "/Volumes/Untitled/SynapticSeaAssets/meshes/source/ship_structural_v0/"
                    "doorway_frame_open_1x1/doorway_frame_open_1x1.blend"
                ),
                "structural",
                (
                    "frame_inner_left",
                    "frame_inner_right",
                    "mechanical_rail_left",
                    "mechanical_rail_right",
                    "seal_left",
                    "seal_right",
                    "threshold_rail",
                    "threshold_seal",
                ),
            ),
            (
                "hull_breach_seal_point",
                Path(
                    "/Volumes/Untitled/SynapticSeaAssets/meshes/source/props/"
                    "hull_breach_seal_point.blend"
                ),
                "prop",
                ("mounting_plate", "hose", "cable", "status"),
            ),
            (
                "fire_suppression_station",
                Path(
                    "/Volumes/Untitled/SynapticSeaAssets/meshes/source/props/"
                    "fire_suppression_station.blend"
                ),
                "prop",
                ("mounting_plate", "handle", "hose", "cable", "indicator"),
            ),
        ],
    )
    def test_landmark_recipes_emit_deterministic_role_inventories_and_canonical_materials(
        tmp_path: Path,
        asset_id: str,
        fixture: Path,
        kind: str,
        tokens: tuple[str, ...],
    ) -> None:
        for candidate in (BLENDER, fixture, MATERIAL_FIXTURE):
            if not candidate.is_file():
                pytest.fail(f"fixture missing: {candidate}")
    
        structural_root = tmp_path / "structural"
        props_root = tmp_path / "props"
        if kind == "structural":
            source_dir = structural_root / asset_id
            source_dir.mkdir(parents=True)
            source = source_dir / f"{asset_id}.blend"
        else:
            structural_root.mkdir(parents=True)
            props_root.mkdir(parents=True)
            source = props_root / f"{asset_id}.blend"
        shutil.copy2(fixture, source)
        _prepare_material_free_source(source)
        material_dir = tmp_path / "materials"
        material_dir.mkdir()
        shutil.copy2(MATERIAL_FIXTURE, material_dir / "salvage_industrial.blend")
    
        command = _recipe_command(
            project_root=PROJECT_ROOT,
            structural_root=structural_root,
            props_root=props_root,
            asset_id=asset_id,
        )
        first = _recipe_with_external_material_library(command)
        second = _recipe_with_external_material_library(command)
>       assert first.returncode == 0, first.stdout + first.stderr
E       AssertionError: 00:00.281  blend            | Read blend: "/Volumes/Untitled/SynapticSeaAssets/worktrees/room-kit-v2/artifacts/room_kit_v2/pipeline-update/tmp/pytest-of-christopherwilloughby/pytest-27/test_landmark_recipes_emit_det0/structural/doorway_frame_open_1x1/doorway_frame_open_1x1.blend"
E         Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14 01:31:22)
E         ERROR: generated object name 'FocusedNine_doorway_frame_open_1x1_jamb_left' is occupied; refusing to co-own an authored or stale object
E         
E       assert 1 == 0
E        +  where 1 = CompletedProcess(args=['/opt/homebrew/bin/blender', '--background', '--factory-startup', '--python', '/Volumes/Untitle...ect name 'FocusedNine_doorway_frame_open_1x1_jamb_left' is occupied; refusing to co-own an authored or stale object\n").returncode

tests/test_focused_nine_blender_recipes.py:2282: AssertionError
=========================== short test summary info ============================
FAILED tests/test_structural_source_contract.py::test_floor_contract_exposes_exact_socket_anchor_names
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[floor_1x1]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[floor_2x1]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[corridor_floor_1x1]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[corridor_floor_1x2]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[wall_straight_1x1]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[doorway_frame_open_1x1]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[pillar_support_1x1]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[ramp_up_1x2]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[bulkhead_portal_2x1]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[ceiling_cap_1x1]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[doorway_frame_blocked_1x1]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[wall_end_cap]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[wall_inner_corner]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[wall_outer_corner]
FAILED tests/test_structural_source_contract.py::test_all_fifteen_modules_load_from_live_contracts[wall_t_junction]
FAILED tests/test_structural_source_contract.py::test_source_record_is_canonical_and_has_no_clock_field
FAILED tests/test_structural_source_contract.py::test_contract_sha256_is_raw_file_hash
FAILED tests/test_structural_source_contract.py::test_socket_z_up_positions_match_contract_after_conversion
FAILED tests/test_structural_source_contract.py::test_inverted_bounds_are_rejected
FAILED tests/test_structural_source_contract.py::test_pressure_door_candidate_reuses_doorway_contract_shape
FAILED tests/test_structural_source_contract.py::test_pressure_door_candidate_rejects_missing_staged_glb
FAILED tests/test_validate_structural_sources.py::test_candidate_source_record_and_report_validate_without_live_selection
FAILED tests/test_validate_structural_sources.py::test_candidate_source_record_hash_mismatch_is_rejected
FAILED tests/test_validate_structural_sources.py::test_candidate_requires_a_real_expected_blend_before_sidecar_passes
FAILED tests/test_validate_structural_sources.py::test_candidate_validates_source_record_identity_and_placement_fields[kit_id-wrong-kit-source record kit_id does not match contract: pressure_door_1x1]
FAILED tests/test_validate_structural_sources.py::test_candidate_validates_source_record_identity_and_placement_fields[coordinate_conversion-wrong-conversion-source record coordinate_conversion does not match contract: pressure_door_1x1]
FAILED tests/test_validate_structural_sources.py::test_candidate_validates_source_record_identity_and_placement_fields[placement_origin-wrong-origin-source record placement_origin does not match contract: pressure_door_1x1]
FAILED tests/test_validate_structural_sources.py::test_candidate_validates_source_record_identity_and_placement_fields[blend_path-/outside/pressure_door_1x1.blend-source record blend_path does not match expected source: pressure_door_1x1]
FAILED tests/test_validate_structural_sources.py::test_candidate_validates_complete_socket_identity_and_metadata
FAILED tests/test_validate_structural_sources.py::test_candidate_rejects_symlinked_blend_leaf
FAILED tests/test_validate_structural_sources.py::test_candidate_rejects_symlinked_source_record_leaf
FAILED tests/test_validate_structural_sources.py::test_candidate_source_record_symlink_loop_is_a_deterministic_error
FAILED tests/test_validate_structural_sources.py::test_candidate_source_module_symlink_loop_is_a_deterministic_validation_error
FAILED tests/test_validate_structural_sources.py::test_candidate_cli_happy_path_emits_pass_without_running_blender
FAILED tests/test_validate_structural_sources.py::test_candidate_cli_rejects_symlinked_record_without_traceback
FAILED tests/test_validate_structural_sources.py::test_candidate_cli_rejects_source_module_loop_without_traceback
FAILED tests/test_validate_structural_variant_bindings.py::ValidateStructuralVariantBindingsTests::test_baseline_structural_fixture_is_accepted
FAILED tests/test_validate_structural_variant_bindings.py::ValidateStructuralVariantBindingsTests::test_non_variant_wrapper_is_ignored_when_it_has_no_integrity_triplet
FAILED tests/test_validate_structural_variant_bindings.py::ValidateStructuralVariantBindingsTests::test_wrapper_path_that_differs_from_variant_path_is_rejected
FAILED tests/test_focused_nine_blender_recipes.py::test_blender_owned_suffixed_library_remnant_is_repaired_canonically
FAILED tests/test_focused_nine_blender_recipes.py::test_blender_verified_remnant_with_wrong_module_context_is_rejected
FAILED tests/test_focused_nine_blender_recipes.py::test_blender_owned_canonical_legacy_material_is_repaired_from_library
FAILED tests/test_focused_nine_blender_recipes.py::test_blender_floor_recipe_emits_exact_material_names_and_exports
FAILED tests/test_focused_nine_blender_recipes.py::test_blender_pressure_door_landmark_roles_are_distinct_and_idempotent
FAILED tests/test_focused_nine_blender_recipes.py::test_blender_floor_recipe_is_idempotent_and_source_scoped
FAILED tests/test_focused_nine_blender_recipes.py::test_blender_compatibility_wrapper_normalizes_blender_arguments
FAILED tests/test_focused_nine_blender_recipes.py::test_blender_under_budget_recipes_meet_gameplay_evidence_minima
FAILED tests/test_focused_nine_blender_recipes.py::test_salvage_detail_recipes_are_named_canonical_and_contract_preserving[floor_1x1]
FAILED tests/test_focused_nine_blender_recipes.py::test_salvage_detail_recipes_are_named_canonical_and_contract_preserving[wall_straight_1x1]
FAILED tests/test_focused_nine_blender_recipes.py::test_salvage_detail_recipes_are_named_canonical_and_contract_preserving[ramp_up_1x2]
FAILED tests/test_focused_nine_blender_recipes.py::test_salvage_detail_recipes_are_named_canonical_and_contract_preserving[pillar_support_1x1]
FAILED tests/test_focused_nine_blender_recipes.py::test_salvage_detail_recipes_are_named_canonical_and_contract_preserving[ceiling_cap_1x1]
FAILED tests/test_focused_nine_blender_recipes.py::test_blender_pillar_details_overlap_body_by_real_world_aabb
FAILED tests/test_focused_nine_blender_recipes.py::test_blender_wall_frame_is_four_rails_without_panel_face_occlusion
FAILED tests/test_focused_nine_blender_recipes.py::test_landmark_recipes_emit_deterministic_role_inventories_and_canonical_materials[doorway_frame_open_1x1-fixture0-structural-tokens0]
ERROR tests/test_validate_structural_sources.py::test_missing_contract_socket_is_reported_deterministically
ERROR tests/test_validate_structural_sources.py::test_non_identity_source_root_is_rejected
ERROR tests/test_validate_structural_sources.py::test_valid_report_passes_validation
ERROR tests/test_validate_structural_sources.py::test_source_record_with_wrong_hash_is_rejected
ERROR tests/test_validate_structural_sources.py::test_extra_helper_is_reported
ERROR tests/test_validate_structural_sources.py::test_wrong_collision_shape_is_reported
ERROR tests/test_validate_structural_sources.py::test_report_errors_are_lexicographically_sorted
56 failed, 62 passed, 7 errors in 43.94s
