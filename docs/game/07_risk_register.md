# 07 Risk Register

| ID | Risk | Likelihood | Impact | Area | Mitigation | Owner | Status |
|---|---|---:|---:|---|---|---|---|
| RISK-001 | Proof-artifact churn displaces real gameplay systems | Medium | High | Process | Enforce docs/game operating model, feature specs, and validation gates | Coordinator | Open |
| RISK-002 | Procedural interiors become spatially incoherent or jumbled | High | High | Design/Tech | Require corridor/socket/topology validation and locked-iso readability checks | Coordinator | Open |
| RISK-003 | Route/system state becomes a god-object | Medium | Medium | Tech | Keep pure state models separated; scene coordinator applies consequences | Coordinator | Mitigated for route control |
| RISK-004 | No-git workspace loses change provenance | Medium | High | Process | Maintain no-git ledgers and Kanban manifests until a repo boundary exists | Coordinator | Open |
| RISK-005 | Workers implement ad-hoc tasks without specs | Medium | High | Process | Require Kanban card contract with source requirements, scope, non-goals, verification | Coordinator | Open |
| RISK-006 | Godot MCP/editor automation changes scenes unsafely | Medium | Medium | Tooling | Prefer code review + headless smokes; keep destructive editor automation scoped | Coordinator | Open |
| RISK-007 | Missing hazard/survival loop leaves slice mechanically flat | Medium | High | Design | Hazard pressure-loop feature spec authored at `features/hazards.md`; oxygen pressure loop implemented and validated by model/main-scene smokes and regression bundle; broader hazard variety remains deferred beyond Gate 1 unless a gate decision recycles scope | Coordinator | Mitigated for Gate 1 |
| RISK-008 | Content production starts before vertical slice proves loop | Medium | High | Production | Gate 1 exit criteria blocks content-scale work | Coordinator | Open |
| RISK-012 | Save corruption wipes player progress (Task 11) | Low | High | Tech | Corruption detection moves bad slot to `user://saves/.corrupt/<slot_id>.<epoch>.bak` instead of silent load failure; index marks the slot `corrupt=true` so the menu shows it as unloadable; `save_slot_state_smoke` locks the contract. | synaptic_seaworker | Mitigated (smoke registered) |
| RISK-013 | Permadeath records accumulate without a UI "forget" affordance (Task 11) | Low | Low | UX/Scope | `PermadeathResolver.clear_death` is the seam; a future menu card (out of scope) can call it. Until then the death file persists but `load_from_slot` returns null so it does not silently replay a dead run. | synaptic_seaworker | Open |
| RISK-009 | Audio layer routes events ad-hoc through stream players instead of named buses | Medium | Medium | Tech | SfxEventRouter + AudioManager service (ADR-0029) centralize routing; every smoke verifies the named-event path; unknown ids are rejected, not silently routed to master | synaptic_seaworker | Mitigated by ADR-0029 |
| RISK-010 | Spatial audio becomes nondeterministic / RNG-driven, breaking headless tests | Medium | Medium | Tech | SpatialAudioResolver is a pure function of (emitter_pos, listener_pos, occluded, base_db); no RNG anywhere in the audio layer; smokes pin exact dB output for canned inputs | synaptic_seaworker | Mitigated by ADR-0029 |
| RISK-011 | EncounterInjector over-fires on critical-path rooms and breaks traversability | Medium | High | Tech | EncounterInjector skips rooms listed in `layout.critical_path` (already populated by `LayoutSerializer._build_critical_path`); the encounter_injector smoke asserts zero markers on critical-path rooms and zero hazard placement on the same set | synaptic_seaworker | Mitigated by ADR-0029 |
| RISK-012 | Biome × difficulty multipliers compose into impossible seeds (hazard density > 3.0) | Medium | High | Gameplay | combined_modifier() clamps the final multiplier to `[0.0, 3.0]`; difficulty_profile_smoke pins the clamp and encounter_injector_smoke asserts hazard density never exceeds the clamp | synaptic_seaworker | Mitigated by ADR-0029 |
| RISK-013 | Layout JSON schema bump (1.1.0 → 1.2.0) breaks external fixtures pinned to 1.1.0 | Low | Medium | Tech | Bump is additive — only new top-level key is `encounters` (Array, possibly empty); GeneratedShipLoader silently ignores unknown keys so 1.1.0 layouts still load | synaptic_seaworker | Mitigated by ADR-0029 |
| RISK-014 | Seed determinism silently regresses across runs / platforms | Low | High | Tech | SeedDeterminismContract.fnv1a_64() and assert_layout_match() smoke runs the full pipeline twice and asserts byte-equal JSON output; the smoke fails the bundle on any drift | synaptic_seaworker | Mitigated by ADR-0029 |
| RISK-009 | Release ops shipped as ad-hoc scripts that only humans can read | Medium | High | Process / Tech | REQ-RL-001..010 lock the package to runtime services + smokes; export-pipeline validator runs in CI before any release build | Coordinator | Mitigated for Gate 3 by Task 13 |
| RISK-010 | Per-run state leaks across runs (achievements, unlocks, demo state) | Medium | High | Tech | ADR-0007 boundary preserved (achievements are per-run, separate file from run snapshot); ADR-0029 explicitly forbids cross-run unlocks in this package; future Steamworks integration reconciles against `user://achievements.json` only | Coordinator | Mitigated for Gate 3 by Task 13 |
| RISK-015 | UI shell introduces a duplicate accessibility seam that drifts from `AccessibilitySettings` | Medium | High | Tech / Accessibility | All new accessibility fields live on the existing `AccessibilitySettings` class (extended additively per ADR-0033); `SettingsState.apply_to_accessibility(a11y)` is the only write-back path; the existing A11Y-P1-001 text-scale smoke and the new `settings_state_smoke` both fail the bundle on any drift | synaptic_seaworker | Mitigated by ADR-0033 |
| RISK-016 | UI shell save/load silently drops settings when an older save is loaded | Low | High | Tech | `RunSnapshot` schema bump 1.0.0 → 1.1.0 with `settings_summary` added; older saves load with `settings_summary = null` and the in-memory state re-bootstraps from default AccessibilitySettings; the new `ui_shell_save_load_smoke` asserts round-trip field-for-field and asserts the older-save load path stays non-fatal | synaptic_seaworker | Mitigated by ADR-0033 |
| RISK-017 | Tutorial banner stacking on rapid triggers creates UI clutter | Low | Medium | UX | `TutorialOverlayPanel` shows only the latest tutorial; `TutorialState.trigger()` is idempotent per `(event, target)` so duplicates never re-fire; the banner auto-hides after 5s or on dismiss; the `main_playable_slice_ui_shell_smoke` asserts banner-stacking invariants | synaptic_seaworker | Mitigated by ADR-0033 |
| RISK-018 | Cross-run meta state is wiped by a deleted current-run save | Low | High | Tech | Per ADR-0033, MetaProgressionState lives in `user://meta_progression.json`, schema `meta-progression-1`, separate from RunSnapshot; `meta_snapshot_smoke` deletes the run save, reloads, and asserts the meta file is untouched | synaptic_seaworker | Mitigated by ADR-0033 |
| RISK-019 | PlayerProgressionState schema drift silently drops cross-training / books_read on older saves | Low | High | Tech | `apply_summary` defaults `cross_training = {}` and `books_read = {}` when the keys are missing; `player_progression_full_smoke` writes a `progression-1` summary, applies it to a `progression-2` configured state, and asserts the new fields stay empty instead of erroring | synaptic_seaworker | Mitigated by ADR-0033 |
| RISK-020 | Hub upgrade purchases can be triggered twice if the meta_state is reloaded mid-frame | Low | Medium | Tech | `purchase` returns true only on a state change; `MetaProgressionState.unlocked_hub_upgrade_ids` is the source of truth, so a duplicate purchase on a fresh reload that already contains the id is a no-op; the `player_progression_full_smoke` exercises the duplicate-purchase path | synaptic_seaworker | Mitigated by ADR-0033 |
| RISK-021 | Spoilage timers drift or reset on save/load, producing phantom fresh/rotten food | Low | High | Tech | `SpoilageState.apply_summary` restores per-item `accumulated_seconds` from the snapshot; `food_save_load_smoke` asserts a 1200s-ticked ration_pack stays FRESH and a cooked_meal stays STALE after round-trip | synaptic_seaworker | Mitigated by ADR-0034 |
| RISK-022 | Cooking produces items without consuming inventory ingredients (duping) | Low | High | Tech | `CookingState.start_cooking` returns `"ok": false` if any ingredient is missing or quantity is insufficient; `cooking_state_smoke` asserts rejections for missing and insufficient ingredients; `main_playable_slice_cooking_smoke` asserts the produced `cooked_meal` item_id matches the recipe | synaptic_seaworker | Mitigated by ADR-0034 |
| RISK-023 | Hydroponics growth cycle resets to PLANTING on reload, wasting water/power | Low | Medium | Tech | `HydroponicsState.apply_summary` restores `progress_seconds` and `stage` enum; `hydroponics_state_smoke` asserts a 30s-ticked crop stays GROWING after round-trip and harvests correctly from HARVESTABLE | synaptic_seaworker | Mitigated by ADR-0034 |
| RISK-024 | Synthesizer bypasses skill requirement via save edit | Low | Medium | Tech | `SynthesizerState.start_synthesis` checks `required_skill_level` against the supplied skill value and returns `"ok": false` if insufficient; `synthesizer_state_smoke` asserts skill rejection and `food_save_load_smoke` asserts the skill-gated round-trip preserves `total_power_consumed` | synaptic_seaworker | Mitigated by ADR-0034 |
| RISK-025 | Food buff/debuff multipliers stack into infinite stat loops | Low | High | Tech | `FoodState` multipliers are clamped to `[0.0, 1.0]` for ROTTEN and `[0.0, 1.0]` for STALE via `get_effective_value()`; `food_state_smoke` asserts rotten_hunger never exceeds fresh_hunger and stale values lie between fresh and rotten | synaptic_seaworker | Mitigated by ADR-0034 |
| RISK-026 | Expanded ship-system package drifts into a coordinator god-object or loses persistence parity | Medium | High | Tech | ADR-0035 keeps power/life-support/hull/fire/propulsion/shield/sustenance logic in pure `RefCounted` models and persists them additively under `ship_systems_summary`; focused smokes assert blackout, telemetry, facility output, and round-trip restoration | synaptic_seaworker | Mitigated by ADR-0035 |
| RISK-027 | Consumable hotbar / save-load drift restores stale item bindings or loses stimulant withdrawal state | Medium | High | Tech | ADR-0036 keeps consumable category state in pure models, `RunSnapshot` persists dedicated summaries, `main_playable_consumables_smoke` asserts hotbar + save/load parity, and `stimulant_state_smoke` proves withdrawal activation/clear behavior | synaptic_seaworker | Mitigated by ADR-0036 |
| RISK-029 | Equipped-item ids drift from combat weapon/ammo ids, causing silent attack failures or wrong ammo spend | Medium | High | Tech | ADR-0037 keeps the combat runtime in pure models but requires `PlayableGeneratedShip` to normalize equipped item ids before attack resolution; `main_playable_slice_combat_encounter_smoke` locks ammo spend, awareness spike, and save/load restoration on a real attack path | synaptic_seaworker | Mitigated by ADR-0037 |
| RISK-030 | Mid-craft save/load can duplicate consumed ingredients or drop in-progress station state | Medium | High | Tech | ADR-0038 persists additive `crafting_summary` + `material_summary` on `RunSnapshot`; `save_load_service_smoke` locks exact field round-trip at `summaries=27`; `main_playable_slice_crafting_smoke` proves a partially progressed craft resumes and completes without ingredient duplication | synaptic_seaworker | Mitigated by ADR-0038 |
| RISK-028 | Loot containers or unique finds respawn after save/load, letting the player duplicate finite rewards | Medium | High | Tech | ADR-0037 keeps loot selection pure, persists searched-container state plus `unique_item_summary` through world snapshots, and locks the contract with `unique_item_state_smoke`, `derelict_loot_smoke`, and `main_playable_slice_loot_ecosystem_smoke` | synaptic_seaworker | Mitigated by ADR-0037 |
| RISK-031 | Cross-system packages pass independently but fail to form a coherent player loop | Medium | High | Product / Validation | ADR-0039 adds `IntegrationMatrix`, `DependencyValidator`, `AutomatedPlaytestRubric`, and focused Task 14 e2e smokes for dependency evidence and seven-stage loop coverage. | synaptic_seareview | Mitigated by Task 14 smokes |
| RISK-032 | Integration audit hides stale documentation, playable-path gaps, or regression blockers behind prose caveats | Medium | High | Process | `ProductAuditReport` requires evidence and fix-card links; Task 15 (`t_c7ac4d08`) tracks final source-map currency, `t_4e47145d` closed the stronger live main-scene/controller e2e probe, and `t_cc483347` closed the rigid-pair regression timeout. | synaptic_seareview | Tracked with explicit cards |
| RISK-033 | Source-of-truth docs or Kanban manifests drift after the E2E wave, causing future workers to rebuild shipped systems or ignore active follow-up cards | Medium | High | Process / Documentation | ADR-0040 adds focused systems-map, requirement-trace, ADR-index, and Kanban-manifest validators; Task 15 registers `SYSTEMS MAP CURRENCY PASS`, `REQUIREMENT TRACE PASS`, and `KANBAN MANIFEST PASS` in the validation plan. | synaptic_seareview | Mitigated by Task 15 validators |
