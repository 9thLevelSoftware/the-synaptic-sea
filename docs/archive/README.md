# Archived documentation — INACCURATE, kept for audit trail only

Quarantined **2026-06-28**.

Every file in this directory describes a fictional or superseded project narrative that
does **not** reflect the real state of the codebase. They were generated by an earlier
automated planning process that:

- invented a "Gate 0–5" stage-gate pipeline and a shipped **v0.1.0 release candidate**
  that never happened (core gameplay systems were still being built *after* the claimed
  release dates — see git history through 2026-06-28);
- hardcoded **macOS-only paths** (`/Users/christopherwilloughby/...`) that do not exist
  on this machine;
- claimed the repo was **"not a git workspace"** (it is);
- used **three mutually contradictory milestone schemes** (Gate 0–5, M0–M3 build-plan,
  and M1–M11 "lanes") — including two different meanings of "M7" and "M1";
- marked every system **"Validated"** when that only meant *unit-tested in isolation*,
  not reachable, driven, or functionally closed-loop.

## Do not trust these files

| File | Why it's wrong |
| --- | --- |
| `08_milestone_gates.md` | Fictional Gate 0–5 pipeline + shipped-RC claim |
| `09_system_roadmap.md` | "All lanes Validated" overstatement; M-numbering conflicts with real specs |
| `PLANNING_SYNTHESIS.md` | Synthesis of the fiction; macOS paths |
| `build-plan.md` | "Not a git workspace" + Gate/M0–M3 board |
| `PROJECT_WORKSPACE.md` | macOS-only project root that doesn't exist here |

## Where the real status lives

- **`/STATUS.md`** — entry point / source of truth pointer.
- **`docs/game/system_completion_audit.md`** — canonical roadmap (code-anchored, re-graded per PR).
- **`docs/game/integration_debt.md`** — reachability ledger.
- **`docs/game/06_validation_plan.md`** — the live validation/smoke contract (still current — *not* archived).
- **`docs/superpowers/specs/` + `plans/`** — real, dated design docs (the trustworthy M7-A/M7-B-era work).
