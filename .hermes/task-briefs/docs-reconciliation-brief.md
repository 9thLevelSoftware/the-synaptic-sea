Docs-only worker. Isolated worktree /Users/christopherwilloughby/Code/the-synaptic-sea-docs, branch docs/evidence-rebind-reconciliation, base = main HEAD 655ae038 of /Users/christopherwilloughby/Code/the-synaptic-sea (create via git worktree add from the main repo; NEVER write inside the main checkout). Python /usr/bin/python3, env PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. PYTEST_DISABLE_PLUGIN_AUTOLOAD=1.

You are reconciling documented pipeline state with the REAL now-integrated state. Ground truth (verify each yourself before writing anything):

- New offline subcommands on tools/meshy_stage.py: `reapprove` (flags: --project-root . --contract data/asset_generation/contracts/loot_container_derelict_v1.json --batch-journal <journal> --reason <nonempty> --operator <nonempty>) - recomputes the live protected snapshot, appends the ORIGINAL approval verbatim to approval_history, fails closed unless COMPLETED and all tasks verified, zero provider calls; ADR-0060 documents it. And `resolve-plan` (flags: --project-root . --contract ... --pricing-file data/asset_generation/meshy_pricing_v1.json --reference-root <task dir> plus four --reference flags front=source_front.png side=source_side.png back=source_back.png three_quarter=source_three_quarter.png) - persists references_resolved=true plus resolved_references and provider_payload_sha256 into the tracked plan envelope preserving other fields, validating against the NEW authoritative schema data/asset_generation/schemas/meshy_plan_envelope_v1.schema.json.
- Live evidence state on main (commit 48202a8b "art: rebind loot container approval evidence offline"): journal 9e04213bc806421d8e64c9c9c23f26d3 now has approval_history with 1 original entry (snapshot 0ad2fc66, size 1922700132 for assets/imported); current approval protected_snapshot is 4842dcc3 size 2528217250, reapproved_at 2026-09-05T19:03:05Z, operator christopher, reason about legitimate post-approval imports. Plan envelope references_resolved=true with 4 resolved references. meshy_stage verify PASSES on main. Selected candidate 01a05dcb verifies promotion_ready. Three siblings verify rejected (superseded by already-selected candidate 01a05dcb). 442 tests pass across the nine meshy suites.
- Mode policy (Blocker 5): Git-tracked evidence leaves are 0644 BY DESIGN (git cannot represent 0600); execution worktrees and external staging rehydrate 0600 files and 0700 dirs before private validation gates; tools enforce 0600 at write time and validators require it at read time. The earlier "0644 blocker" finding is resolved by this documented policy, not by chmodding tracked files.
- D5/D6/D8 evidence now on main: task-local cleaned.glb (792 triangles, 2 materials, UVs, raw preserved), blender-validation.json, review.json promotion_ready, 18 runtime PNGs plus runtime-review.json (seeds 42 and 777 across normal/emergency/dark), sidecar-overlay.json proposal_only=true targeting res://assets/imported/props/dressing/loot_container_derelict_v1.sidecar.json which is NOT written - promotion remains proposal-only.

TASK - update ONLY these files in your worktree:
1. docs/game/06_validation_plan.md - around lines 53-54 there is stale references_resolved=false wording (update to reflect the persisted resolved envelope); add reapprove/resolve-plan to the lifecycle command block (lines ~65-94) with exact commands; reconcile the runtime/proposal block (lines ~128-150) - the only remaining loot-container gap is that the promotion proposal is staged but not applied.
2. docs/superpowers/proofs/asset-pipeline-closure.md - update the evidence inventory (lines ~114-167) to include the D5/D6/D8 artifacts and the reapprove/resolve-plan results; update the command/exit inventory (lines ~179-203) with the new commands and their live exit codes (all PASS); update known blockers (lines ~226-239): REMOVE the three resolved blockers (protected-surface drift now reapproved offline with append-only history; three pending reviews now rejected as superseded; references_resolved=false now persisted), REPLACE with the honest remaining item (promotion proposal staged; application to live assets/imported is a separate future operation per plan Task I).
3. docs/game/features/ai_candidate_asset_pipeline.md - staging layout section (lines ~145-166): add one short paragraph noting the mode policy (tracked 0644 by design; 0600/0700 rehydrated in execution and external contexts).
4. docs/game/adr/0060-meshy-offline-evidence-rebind.md - if it lacks a live-use note, add ONE sentence noting the first live use (batch 9e04213b, 2026-09-05). Do not rewrite the ADR.

Do NOT edit .hermes/plans/, no tools/tests/schemas changes, no provider/generation/promotion.

VERIFY: read back each edited section for factual accuracy (run the actual commands where cheap: meshy_stage verify exit 0, candidate verify outputs) - do NOT document anything you did not observe. Run the nine meshy test suites once (442 expected). git diff --check. Commit single: "docs: reconcile evidence rebind pipeline state". Report the exact sections changed and the verification evidence.